// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yoshiso/hypersync/ent/delegatorreward"
	"github.com/yoshiso/hypersync/ent/predicate"
)

// DelegatorRewardQuery is the builder for querying DelegatorReward entities.
type DelegatorRewardQuery struct {
	config
	ctx        *QueryContext
	order      []delegatorreward.OrderOption
	inters     []Interceptor
	predicates []predicate.DelegatorReward
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DelegatorRewardQuery builder.
func (drq *DelegatorRewardQuery) Where(ps ...predicate.DelegatorReward) *DelegatorRewardQuery {
	drq.predicates = append(drq.predicates, ps...)
	return drq
}

// Limit the number of records to be returned by this query.
func (drq *DelegatorRewardQuery) Limit(limit int) *DelegatorRewardQuery {
	drq.ctx.Limit = &limit
	return drq
}

// Offset to start from.
func (drq *DelegatorRewardQuery) Offset(offset int) *DelegatorRewardQuery {
	drq.ctx.Offset = &offset
	return drq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (drq *DelegatorRewardQuery) Unique(unique bool) *DelegatorRewardQuery {
	drq.ctx.Unique = &unique
	return drq
}

// Order specifies how the records should be ordered.
func (drq *DelegatorRewardQuery) Order(o ...delegatorreward.OrderOption) *DelegatorRewardQuery {
	drq.order = append(drq.order, o...)
	return drq
}

// First returns the first DelegatorReward entity from the query.
// Returns a *NotFoundError when no DelegatorReward was found.
func (drq *DelegatorRewardQuery) First(ctx context.Context) (*DelegatorReward, error) {
	nodes, err := drq.Limit(1).All(setContextOp(ctx, drq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{delegatorreward.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (drq *DelegatorRewardQuery) FirstX(ctx context.Context) *DelegatorReward {
	node, err := drq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DelegatorReward ID from the query.
// Returns a *NotFoundError when no DelegatorReward ID was found.
func (drq *DelegatorRewardQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = drq.Limit(1).IDs(setContextOp(ctx, drq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{delegatorreward.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (drq *DelegatorRewardQuery) FirstIDX(ctx context.Context) int {
	id, err := drq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DelegatorReward entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DelegatorReward entity is found.
// Returns a *NotFoundError when no DelegatorReward entities are found.
func (drq *DelegatorRewardQuery) Only(ctx context.Context) (*DelegatorReward, error) {
	nodes, err := drq.Limit(2).All(setContextOp(ctx, drq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{delegatorreward.Label}
	default:
		return nil, &NotSingularError{delegatorreward.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (drq *DelegatorRewardQuery) OnlyX(ctx context.Context) *DelegatorReward {
	node, err := drq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DelegatorReward ID in the query.
// Returns a *NotSingularError when more than one DelegatorReward ID is found.
// Returns a *NotFoundError when no entities are found.
func (drq *DelegatorRewardQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = drq.Limit(2).IDs(setContextOp(ctx, drq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{delegatorreward.Label}
	default:
		err = &NotSingularError{delegatorreward.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (drq *DelegatorRewardQuery) OnlyIDX(ctx context.Context) int {
	id, err := drq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DelegatorRewards.
func (drq *DelegatorRewardQuery) All(ctx context.Context) ([]*DelegatorReward, error) {
	ctx = setContextOp(ctx, drq.ctx, ent.OpQueryAll)
	if err := drq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DelegatorReward, *DelegatorRewardQuery]()
	return withInterceptors[[]*DelegatorReward](ctx, drq, qr, drq.inters)
}

// AllX is like All, but panics if an error occurs.
func (drq *DelegatorRewardQuery) AllX(ctx context.Context) []*DelegatorReward {
	nodes, err := drq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DelegatorReward IDs.
func (drq *DelegatorRewardQuery) IDs(ctx context.Context) (ids []int, err error) {
	if drq.ctx.Unique == nil && drq.path != nil {
		drq.Unique(true)
	}
	ctx = setContextOp(ctx, drq.ctx, ent.OpQueryIDs)
	if err = drq.Select(delegatorreward.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (drq *DelegatorRewardQuery) IDsX(ctx context.Context) []int {
	ids, err := drq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (drq *DelegatorRewardQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, drq.ctx, ent.OpQueryCount)
	if err := drq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, drq, querierCount[*DelegatorRewardQuery](), drq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (drq *DelegatorRewardQuery) CountX(ctx context.Context) int {
	count, err := drq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (drq *DelegatorRewardQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, drq.ctx, ent.OpQueryExist)
	switch _, err := drq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (drq *DelegatorRewardQuery) ExistX(ctx context.Context) bool {
	exist, err := drq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DelegatorRewardQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (drq *DelegatorRewardQuery) Clone() *DelegatorRewardQuery {
	if drq == nil {
		return nil
	}
	return &DelegatorRewardQuery{
		config:     drq.config,
		ctx:        drq.ctx.Clone(),
		order:      append([]delegatorreward.OrderOption{}, drq.order...),
		inters:     append([]Interceptor{}, drq.inters...),
		predicates: append([]predicate.DelegatorReward{}, drq.predicates...),
		// clone intermediate query.
		sql:  drq.sql.Clone(),
		path: drq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Source string `json:"source,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DelegatorReward.Query().
//		GroupBy(delegatorreward.FieldSource).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (drq *DelegatorRewardQuery) GroupBy(field string, fields ...string) *DelegatorRewardGroupBy {
	drq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DelegatorRewardGroupBy{build: drq}
	grbuild.flds = &drq.ctx.Fields
	grbuild.label = delegatorreward.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Source string `json:"source,omitempty"`
//	}
//
//	client.DelegatorReward.Query().
//		Select(delegatorreward.FieldSource).
//		Scan(ctx, &v)
func (drq *DelegatorRewardQuery) Select(fields ...string) *DelegatorRewardSelect {
	drq.ctx.Fields = append(drq.ctx.Fields, fields...)
	sbuild := &DelegatorRewardSelect{DelegatorRewardQuery: drq}
	sbuild.label = delegatorreward.Label
	sbuild.flds, sbuild.scan = &drq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DelegatorRewardSelect configured with the given aggregations.
func (drq *DelegatorRewardQuery) Aggregate(fns ...AggregateFunc) *DelegatorRewardSelect {
	return drq.Select().Aggregate(fns...)
}

func (drq *DelegatorRewardQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range drq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, drq); err != nil {
				return err
			}
		}
	}
	for _, f := range drq.ctx.Fields {
		if !delegatorreward.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if drq.path != nil {
		prev, err := drq.path(ctx)
		if err != nil {
			return err
		}
		drq.sql = prev
	}
	return nil
}

func (drq *DelegatorRewardQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DelegatorReward, error) {
	var (
		nodes = []*DelegatorReward{}
		_spec = drq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DelegatorReward).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DelegatorReward{config: drq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, drq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (drq *DelegatorRewardQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := drq.querySpec()
	_spec.Node.Columns = drq.ctx.Fields
	if len(drq.ctx.Fields) > 0 {
		_spec.Unique = drq.ctx.Unique != nil && *drq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, drq.driver, _spec)
}

func (drq *DelegatorRewardQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(delegatorreward.Table, delegatorreward.Columns, sqlgraph.NewFieldSpec(delegatorreward.FieldID, field.TypeInt))
	_spec.From = drq.sql
	if unique := drq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if drq.path != nil {
		_spec.Unique = true
	}
	if fields := drq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, delegatorreward.FieldID)
		for i := range fields {
			if fields[i] != delegatorreward.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := drq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := drq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := drq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := drq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (drq *DelegatorRewardQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(drq.driver.Dialect())
	t1 := builder.Table(delegatorreward.Table)
	columns := drq.ctx.Fields
	if len(columns) == 0 {
		columns = delegatorreward.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if drq.sql != nil {
		selector = drq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if drq.ctx.Unique != nil && *drq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range drq.predicates {
		p(selector)
	}
	for _, p := range drq.order {
		p(selector)
	}
	if offset := drq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := drq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DelegatorRewardGroupBy is the group-by builder for DelegatorReward entities.
type DelegatorRewardGroupBy struct {
	selector
	build *DelegatorRewardQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (drgb *DelegatorRewardGroupBy) Aggregate(fns ...AggregateFunc) *DelegatorRewardGroupBy {
	drgb.fns = append(drgb.fns, fns...)
	return drgb
}

// Scan applies the selector query and scans the result into the given value.
func (drgb *DelegatorRewardGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, drgb.build.ctx, ent.OpQueryGroupBy)
	if err := drgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DelegatorRewardQuery, *DelegatorRewardGroupBy](ctx, drgb.build, drgb, drgb.build.inters, v)
}

func (drgb *DelegatorRewardGroupBy) sqlScan(ctx context.Context, root *DelegatorRewardQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(drgb.fns))
	for _, fn := range drgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*drgb.flds)+len(drgb.fns))
		for _, f := range *drgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*drgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := drgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DelegatorRewardSelect is the builder for selecting fields of DelegatorReward entities.
type DelegatorRewardSelect struct {
	*DelegatorRewardQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (drs *DelegatorRewardSelect) Aggregate(fns ...AggregateFunc) *DelegatorRewardSelect {
	drs.fns = append(drs.fns, fns...)
	return drs
}

// Scan applies the selector query and scans the result into the given value.
func (drs *DelegatorRewardSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, drs.ctx, ent.OpQuerySelect)
	if err := drs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DelegatorRewardQuery, *DelegatorRewardSelect](ctx, drs.DelegatorRewardQuery, drs, drs.inters, v)
}

func (drs *DelegatorRewardSelect) sqlScan(ctx context.Context, root *DelegatorRewardQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(drs.fns))
	for _, fn := range drs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*drs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := drs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
