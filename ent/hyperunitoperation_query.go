// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yoshiso/hypersync/ent/hyperunitoperation"
	"github.com/yoshiso/hypersync/ent/predicate"
)

// HyperunitOperationQuery is the builder for querying HyperunitOperation entities.
type HyperunitOperationQuery struct {
	config
	ctx        *QueryContext
	order      []hyperunitoperation.OrderOption
	inters     []Interceptor
	predicates []predicate.HyperunitOperation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HyperunitOperationQuery builder.
func (hoq *HyperunitOperationQuery) Where(ps ...predicate.HyperunitOperation) *HyperunitOperationQuery {
	hoq.predicates = append(hoq.predicates, ps...)
	return hoq
}

// Limit the number of records to be returned by this query.
func (hoq *HyperunitOperationQuery) Limit(limit int) *HyperunitOperationQuery {
	hoq.ctx.Limit = &limit
	return hoq
}

// Offset to start from.
func (hoq *HyperunitOperationQuery) Offset(offset int) *HyperunitOperationQuery {
	hoq.ctx.Offset = &offset
	return hoq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hoq *HyperunitOperationQuery) Unique(unique bool) *HyperunitOperationQuery {
	hoq.ctx.Unique = &unique
	return hoq
}

// Order specifies how the records should be ordered.
func (hoq *HyperunitOperationQuery) Order(o ...hyperunitoperation.OrderOption) *HyperunitOperationQuery {
	hoq.order = append(hoq.order, o...)
	return hoq
}

// First returns the first HyperunitOperation entity from the query.
// Returns a *NotFoundError when no HyperunitOperation was found.
func (hoq *HyperunitOperationQuery) First(ctx context.Context) (*HyperunitOperation, error) {
	nodes, err := hoq.Limit(1).All(setContextOp(ctx, hoq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hyperunitoperation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) FirstX(ctx context.Context) *HyperunitOperation {
	node, err := hoq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HyperunitOperation ID from the query.
// Returns a *NotFoundError when no HyperunitOperation ID was found.
func (hoq *HyperunitOperationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hoq.Limit(1).IDs(setContextOp(ctx, hoq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hyperunitoperation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) FirstIDX(ctx context.Context) int {
	id, err := hoq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HyperunitOperation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HyperunitOperation entity is found.
// Returns a *NotFoundError when no HyperunitOperation entities are found.
func (hoq *HyperunitOperationQuery) Only(ctx context.Context) (*HyperunitOperation, error) {
	nodes, err := hoq.Limit(2).All(setContextOp(ctx, hoq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hyperunitoperation.Label}
	default:
		return nil, &NotSingularError{hyperunitoperation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) OnlyX(ctx context.Context) *HyperunitOperation {
	node, err := hoq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HyperunitOperation ID in the query.
// Returns a *NotSingularError when more than one HyperunitOperation ID is found.
// Returns a *NotFoundError when no entities are found.
func (hoq *HyperunitOperationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = hoq.Limit(2).IDs(setContextOp(ctx, hoq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hyperunitoperation.Label}
	default:
		err = &NotSingularError{hyperunitoperation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) OnlyIDX(ctx context.Context) int {
	id, err := hoq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HyperunitOperations.
func (hoq *HyperunitOperationQuery) All(ctx context.Context) ([]*HyperunitOperation, error) {
	ctx = setContextOp(ctx, hoq.ctx, ent.OpQueryAll)
	if err := hoq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HyperunitOperation, *HyperunitOperationQuery]()
	return withInterceptors[[]*HyperunitOperation](ctx, hoq, qr, hoq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) AllX(ctx context.Context) []*HyperunitOperation {
	nodes, err := hoq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HyperunitOperation IDs.
func (hoq *HyperunitOperationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if hoq.ctx.Unique == nil && hoq.path != nil {
		hoq.Unique(true)
	}
	ctx = setContextOp(ctx, hoq.ctx, ent.OpQueryIDs)
	if err = hoq.Select(hyperunitoperation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) IDsX(ctx context.Context) []int {
	ids, err := hoq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hoq *HyperunitOperationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hoq.ctx, ent.OpQueryCount)
	if err := hoq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hoq, querierCount[*HyperunitOperationQuery](), hoq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) CountX(ctx context.Context) int {
	count, err := hoq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hoq *HyperunitOperationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hoq.ctx, ent.OpQueryExist)
	switch _, err := hoq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hoq *HyperunitOperationQuery) ExistX(ctx context.Context) bool {
	exist, err := hoq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HyperunitOperationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hoq *HyperunitOperationQuery) Clone() *HyperunitOperationQuery {
	if hoq == nil {
		return nil
	}
	return &HyperunitOperationQuery{
		config:     hoq.config,
		ctx:        hoq.ctx.Clone(),
		order:      append([]hyperunitoperation.OrderOption{}, hoq.order...),
		inters:     append([]Interceptor{}, hoq.inters...),
		predicates: append([]predicate.HyperunitOperation{}, hoq.predicates...),
		// clone intermediate query.
		sql:  hoq.sql.Clone(),
		path: hoq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HyperunitOperation.Query().
//		GroupBy(hyperunitoperation.FieldAddress).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hoq *HyperunitOperationQuery) GroupBy(field string, fields ...string) *HyperunitOperationGroupBy {
	hoq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HyperunitOperationGroupBy{build: hoq}
	grbuild.flds = &hoq.ctx.Fields
	grbuild.label = hyperunitoperation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Address string `json:"address,omitempty"`
//	}
//
//	client.HyperunitOperation.Query().
//		Select(hyperunitoperation.FieldAddress).
//		Scan(ctx, &v)
func (hoq *HyperunitOperationQuery) Select(fields ...string) *HyperunitOperationSelect {
	hoq.ctx.Fields = append(hoq.ctx.Fields, fields...)
	sbuild := &HyperunitOperationSelect{HyperunitOperationQuery: hoq}
	sbuild.label = hyperunitoperation.Label
	sbuild.flds, sbuild.scan = &hoq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HyperunitOperationSelect configured with the given aggregations.
func (hoq *HyperunitOperationQuery) Aggregate(fns ...AggregateFunc) *HyperunitOperationSelect {
	return hoq.Select().Aggregate(fns...)
}

func (hoq *HyperunitOperationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hoq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hoq); err != nil {
				return err
			}
		}
	}
	for _, f := range hoq.ctx.Fields {
		if !hyperunitoperation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hoq.path != nil {
		prev, err := hoq.path(ctx)
		if err != nil {
			return err
		}
		hoq.sql = prev
	}
	return nil
}

func (hoq *HyperunitOperationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HyperunitOperation, error) {
	var (
		nodes = []*HyperunitOperation{}
		_spec = hoq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HyperunitOperation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HyperunitOperation{config: hoq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hoq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (hoq *HyperunitOperationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hoq.querySpec()
	_spec.Node.Columns = hoq.ctx.Fields
	if len(hoq.ctx.Fields) > 0 {
		_spec.Unique = hoq.ctx.Unique != nil && *hoq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hoq.driver, _spec)
}

func (hoq *HyperunitOperationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hyperunitoperation.Table, hyperunitoperation.Columns, sqlgraph.NewFieldSpec(hyperunitoperation.FieldID, field.TypeInt))
	_spec.From = hoq.sql
	if unique := hoq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hoq.path != nil {
		_spec.Unique = true
	}
	if fields := hoq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hyperunitoperation.FieldID)
		for i := range fields {
			if fields[i] != hyperunitoperation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hoq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hoq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hoq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hoq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hoq *HyperunitOperationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hoq.driver.Dialect())
	t1 := builder.Table(hyperunitoperation.Table)
	columns := hoq.ctx.Fields
	if len(columns) == 0 {
		columns = hyperunitoperation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hoq.sql != nil {
		selector = hoq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hoq.ctx.Unique != nil && *hoq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hoq.predicates {
		p(selector)
	}
	for _, p := range hoq.order {
		p(selector)
	}
	if offset := hoq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hoq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HyperunitOperationGroupBy is the group-by builder for HyperunitOperation entities.
type HyperunitOperationGroupBy struct {
	selector
	build *HyperunitOperationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hogb *HyperunitOperationGroupBy) Aggregate(fns ...AggregateFunc) *HyperunitOperationGroupBy {
	hogb.fns = append(hogb.fns, fns...)
	return hogb
}

// Scan applies the selector query and scans the result into the given value.
func (hogb *HyperunitOperationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hogb.build.ctx, ent.OpQueryGroupBy)
	if err := hogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HyperunitOperationQuery, *HyperunitOperationGroupBy](ctx, hogb.build, hogb, hogb.build.inters, v)
}

func (hogb *HyperunitOperationGroupBy) sqlScan(ctx context.Context, root *HyperunitOperationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hogb.fns))
	for _, fn := range hogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hogb.flds)+len(hogb.fns))
		for _, f := range *hogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HyperunitOperationSelect is the builder for selecting fields of HyperunitOperation entities.
type HyperunitOperationSelect struct {
	*HyperunitOperationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hos *HyperunitOperationSelect) Aggregate(fns ...AggregateFunc) *HyperunitOperationSelect {
	hos.fns = append(hos.fns, fns...)
	return hos
}

// Scan applies the selector query and scans the result into the given value.
func (hos *HyperunitOperationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hos.ctx, ent.OpQuerySelect)
	if err := hos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HyperunitOperationQuery, *HyperunitOperationSelect](ctx, hos.HyperunitOperationQuery, hos, hos.inters, v)
}

func (hos *HyperunitOperationSelect) sqlScan(ctx context.Context, root *HyperunitOperationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hos.fns))
	for _, fn := range hos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
