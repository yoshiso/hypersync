// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/yoshiso/hypersync/ent/predicate"
	"github.com/yoshiso/hypersync/ent/vaultleadercommission"
)

// VaultLeaderCommissionQuery is the builder for querying VaultLeaderCommission entities.
type VaultLeaderCommissionQuery struct {
	config
	ctx        *QueryContext
	order      []vaultleadercommission.OrderOption
	inters     []Interceptor
	predicates []predicate.VaultLeaderCommission
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the VaultLeaderCommissionQuery builder.
func (vlcq *VaultLeaderCommissionQuery) Where(ps ...predicate.VaultLeaderCommission) *VaultLeaderCommissionQuery {
	vlcq.predicates = append(vlcq.predicates, ps...)
	return vlcq
}

// Limit the number of records to be returned by this query.
func (vlcq *VaultLeaderCommissionQuery) Limit(limit int) *VaultLeaderCommissionQuery {
	vlcq.ctx.Limit = &limit
	return vlcq
}

// Offset to start from.
func (vlcq *VaultLeaderCommissionQuery) Offset(offset int) *VaultLeaderCommissionQuery {
	vlcq.ctx.Offset = &offset
	return vlcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (vlcq *VaultLeaderCommissionQuery) Unique(unique bool) *VaultLeaderCommissionQuery {
	vlcq.ctx.Unique = &unique
	return vlcq
}

// Order specifies how the records should be ordered.
func (vlcq *VaultLeaderCommissionQuery) Order(o ...vaultleadercommission.OrderOption) *VaultLeaderCommissionQuery {
	vlcq.order = append(vlcq.order, o...)
	return vlcq
}

// First returns the first VaultLeaderCommission entity from the query.
// Returns a *NotFoundError when no VaultLeaderCommission was found.
func (vlcq *VaultLeaderCommissionQuery) First(ctx context.Context) (*VaultLeaderCommission, error) {
	nodes, err := vlcq.Limit(1).All(setContextOp(ctx, vlcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{vaultleadercommission.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) FirstX(ctx context.Context) *VaultLeaderCommission {
	node, err := vlcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first VaultLeaderCommission ID from the query.
// Returns a *NotFoundError when no VaultLeaderCommission ID was found.
func (vlcq *VaultLeaderCommissionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vlcq.Limit(1).IDs(setContextOp(ctx, vlcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{vaultleadercommission.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) FirstIDX(ctx context.Context) int {
	id, err := vlcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single VaultLeaderCommission entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one VaultLeaderCommission entity is found.
// Returns a *NotFoundError when no VaultLeaderCommission entities are found.
func (vlcq *VaultLeaderCommissionQuery) Only(ctx context.Context) (*VaultLeaderCommission, error) {
	nodes, err := vlcq.Limit(2).All(setContextOp(ctx, vlcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{vaultleadercommission.Label}
	default:
		return nil, &NotSingularError{vaultleadercommission.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) OnlyX(ctx context.Context) *VaultLeaderCommission {
	node, err := vlcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only VaultLeaderCommission ID in the query.
// Returns a *NotSingularError when more than one VaultLeaderCommission ID is found.
// Returns a *NotFoundError when no entities are found.
func (vlcq *VaultLeaderCommissionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = vlcq.Limit(2).IDs(setContextOp(ctx, vlcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{vaultleadercommission.Label}
	default:
		err = &NotSingularError{vaultleadercommission.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) OnlyIDX(ctx context.Context) int {
	id, err := vlcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of VaultLeaderCommissions.
func (vlcq *VaultLeaderCommissionQuery) All(ctx context.Context) ([]*VaultLeaderCommission, error) {
	ctx = setContextOp(ctx, vlcq.ctx, ent.OpQueryAll)
	if err := vlcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*VaultLeaderCommission, *VaultLeaderCommissionQuery]()
	return withInterceptors[[]*VaultLeaderCommission](ctx, vlcq, qr, vlcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) AllX(ctx context.Context) []*VaultLeaderCommission {
	nodes, err := vlcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of VaultLeaderCommission IDs.
func (vlcq *VaultLeaderCommissionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if vlcq.ctx.Unique == nil && vlcq.path != nil {
		vlcq.Unique(true)
	}
	ctx = setContextOp(ctx, vlcq.ctx, ent.OpQueryIDs)
	if err = vlcq.Select(vaultleadercommission.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) IDsX(ctx context.Context) []int {
	ids, err := vlcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (vlcq *VaultLeaderCommissionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, vlcq.ctx, ent.OpQueryCount)
	if err := vlcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, vlcq, querierCount[*VaultLeaderCommissionQuery](), vlcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) CountX(ctx context.Context) int {
	count, err := vlcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (vlcq *VaultLeaderCommissionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, vlcq.ctx, ent.OpQueryExist)
	switch _, err := vlcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (vlcq *VaultLeaderCommissionQuery) ExistX(ctx context.Context) bool {
	exist, err := vlcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the VaultLeaderCommissionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (vlcq *VaultLeaderCommissionQuery) Clone() *VaultLeaderCommissionQuery {
	if vlcq == nil {
		return nil
	}
	return &VaultLeaderCommissionQuery{
		config:     vlcq.config,
		ctx:        vlcq.ctx.Clone(),
		order:      append([]vaultleadercommission.OrderOption{}, vlcq.order...),
		inters:     append([]Interceptor{}, vlcq.inters...),
		predicates: append([]predicate.VaultLeaderCommission{}, vlcq.predicates...),
		// clone intermediate query.
		sql:  vlcq.sql.Clone(),
		path: vlcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		User string `json:"user,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.VaultLeaderCommission.Query().
//		GroupBy(vaultleadercommission.FieldUser).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (vlcq *VaultLeaderCommissionQuery) GroupBy(field string, fields ...string) *VaultLeaderCommissionGroupBy {
	vlcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &VaultLeaderCommissionGroupBy{build: vlcq}
	grbuild.flds = &vlcq.ctx.Fields
	grbuild.label = vaultleadercommission.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		User string `json:"user,omitempty"`
//	}
//
//	client.VaultLeaderCommission.Query().
//		Select(vaultleadercommission.FieldUser).
//		Scan(ctx, &v)
func (vlcq *VaultLeaderCommissionQuery) Select(fields ...string) *VaultLeaderCommissionSelect {
	vlcq.ctx.Fields = append(vlcq.ctx.Fields, fields...)
	sbuild := &VaultLeaderCommissionSelect{VaultLeaderCommissionQuery: vlcq}
	sbuild.label = vaultleadercommission.Label
	sbuild.flds, sbuild.scan = &vlcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a VaultLeaderCommissionSelect configured with the given aggregations.
func (vlcq *VaultLeaderCommissionQuery) Aggregate(fns ...AggregateFunc) *VaultLeaderCommissionSelect {
	return vlcq.Select().Aggregate(fns...)
}

func (vlcq *VaultLeaderCommissionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range vlcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, vlcq); err != nil {
				return err
			}
		}
	}
	for _, f := range vlcq.ctx.Fields {
		if !vaultleadercommission.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if vlcq.path != nil {
		prev, err := vlcq.path(ctx)
		if err != nil {
			return err
		}
		vlcq.sql = prev
	}
	return nil
}

func (vlcq *VaultLeaderCommissionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*VaultLeaderCommission, error) {
	var (
		nodes = []*VaultLeaderCommission{}
		_spec = vlcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*VaultLeaderCommission).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &VaultLeaderCommission{config: vlcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, vlcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (vlcq *VaultLeaderCommissionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := vlcq.querySpec()
	_spec.Node.Columns = vlcq.ctx.Fields
	if len(vlcq.ctx.Fields) > 0 {
		_spec.Unique = vlcq.ctx.Unique != nil && *vlcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, vlcq.driver, _spec)
}

func (vlcq *VaultLeaderCommissionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(vaultleadercommission.Table, vaultleadercommission.Columns, sqlgraph.NewFieldSpec(vaultleadercommission.FieldID, field.TypeInt))
	_spec.From = vlcq.sql
	if unique := vlcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if vlcq.path != nil {
		_spec.Unique = true
	}
	if fields := vlcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, vaultleadercommission.FieldID)
		for i := range fields {
			if fields[i] != vaultleadercommission.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := vlcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := vlcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := vlcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := vlcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (vlcq *VaultLeaderCommissionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(vlcq.driver.Dialect())
	t1 := builder.Table(vaultleadercommission.Table)
	columns := vlcq.ctx.Fields
	if len(columns) == 0 {
		columns = vaultleadercommission.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if vlcq.sql != nil {
		selector = vlcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if vlcq.ctx.Unique != nil && *vlcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range vlcq.predicates {
		p(selector)
	}
	for _, p := range vlcq.order {
		p(selector)
	}
	if offset := vlcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := vlcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// VaultLeaderCommissionGroupBy is the group-by builder for VaultLeaderCommission entities.
type VaultLeaderCommissionGroupBy struct {
	selector
	build *VaultLeaderCommissionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (vlcgb *VaultLeaderCommissionGroupBy) Aggregate(fns ...AggregateFunc) *VaultLeaderCommissionGroupBy {
	vlcgb.fns = append(vlcgb.fns, fns...)
	return vlcgb
}

// Scan applies the selector query and scans the result into the given value.
func (vlcgb *VaultLeaderCommissionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vlcgb.build.ctx, ent.OpQueryGroupBy)
	if err := vlcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VaultLeaderCommissionQuery, *VaultLeaderCommissionGroupBy](ctx, vlcgb.build, vlcgb, vlcgb.build.inters, v)
}

func (vlcgb *VaultLeaderCommissionGroupBy) sqlScan(ctx context.Context, root *VaultLeaderCommissionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(vlcgb.fns))
	for _, fn := range vlcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*vlcgb.flds)+len(vlcgb.fns))
		for _, f := range *vlcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*vlcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vlcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// VaultLeaderCommissionSelect is the builder for selecting fields of VaultLeaderCommission entities.
type VaultLeaderCommissionSelect struct {
	*VaultLeaderCommissionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (vlcs *VaultLeaderCommissionSelect) Aggregate(fns ...AggregateFunc) *VaultLeaderCommissionSelect {
	vlcs.fns = append(vlcs.fns, fns...)
	return vlcs
}

// Scan applies the selector query and scans the result into the given value.
func (vlcs *VaultLeaderCommissionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, vlcs.ctx, ent.OpQuerySelect)
	if err := vlcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*VaultLeaderCommissionQuery, *VaultLeaderCommissionSelect](ctx, vlcs.VaultLeaderCommissionQuery, vlcs, vlcs.inters, v)
}

func (vlcs *VaultLeaderCommissionSelect) sqlScan(ctx context.Context, root *VaultLeaderCommissionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(vlcs.fns))
	for _, fn := range vlcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*vlcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := vlcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
